// Number of rows per batch for streams (currently r.range() only)
module.exports.ROW_PER_BATCH = 40;

// Print debug output
module.exports.DEBUG = false;

// Enable r.js
module.exports.ENABLE_JS = true;

// Print RethinkDB's info
module.exports.SILENT = false;
